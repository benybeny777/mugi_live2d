# むぎ Live2D モデル制作 作業規約

このリポジトリでは、むぎのモデル本体、生成PSD、Cubismデータ、制作手順を一緒に管理する。

## 長時間処理とログ

- 1分以上かかる処理は、開始前に推定時間と処理内容を報告する。
- 作業中は5分以内を目安に、現在の工程と正常性を簡潔に報告する。
- Python処理は非バッファリングモードで起動し、標準出力、標準エラー、開始・終了時刻、終了コードをファイルへ保存する。
- CPU、GPU、メモリ、生成ファイル数と容量を定期記録する。
- プロセスが存在するだけで正常と判断しない。ログ更新、CPU/GPU活動、出力更新を合わせて確認する。
- 失敗時はエラーログと終了コードから原因を特定する。同じ設定での無根拠な再試行をしない。
- PowerShellから外部コマンドを実行するときは、警告のstderr出力をPowerShell例外として誤判定しないよう、`Start-Process` の出力リダイレクトを使う。

## このPCのSee-through設定

- GPUはVRAM 8GBのため、標準実行は公式の8GB向けblockswap経路を使う。NF4経路を使う場合はgroup offloadとCPU offloadを有効にする。
- 初期解像度はLayerDiff 1024、Depth 512とし、安定完走を確認してから品質設定を上げる。
- CUDAメモリ不足が出た場合は、まず他のGPUプロセスとログを確認し、解像度変更は記録してから行う。
- セットアップ後に `scripts/apply-seethrough-local-fixes.ps1` を実行し、CPU offload時の入力テンソルをCUDA実行デバイスへ合わせる。
- 生成物は `work/psd/seethrough/`、実行ログは `logs/` に保存する。

## データ管理

- PSD、CMO3、MOC3、PNGなど大容量のLive2D制作データはGit LFSで管理する。
- 完成データだけでなく、再編集可能なPSD、Cubismプロジェクト、書き出し設定、検証記録を残す。
- 公式サンプルなど再配布条件が不明なデータはGitへ含めず、ローカル参照として管理する。
- キャラクターを追加するときも `NEW_CHARACTER.md` と `WORKFLOW.md` を使い、手順を常に最新に保つ。
