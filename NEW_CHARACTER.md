# 別キャラクターを作るときの手順

このPCで、むぎと同じ方式のLive2Dモデルを別キャラクターへ再利用するためのチェックリストです。

## 新しい管理場所を作る

1. `C:\00_PG\40_mugi_live2d` を直接流用・上書きしない。
2. 次の空き番号で `C:\00_PG\NN_<character>_live2d` を作る。
3. このリポジトリの文書、ディレクトリ構成、`.gitignore`、`.gitattributes` をコピーする。
4. `source/<character>-original.png` に権利を持つ元画像を置く。
5. `git init -b main` と `git lfs install --local` を実行し、PSD、CMO3、MOC3、PNGをLFS対象にする。

## 共通制作工程

1. See-throughの8GB向けNF4経路で元画像を意味レイヤーPSDへ分解し、新キャラ側の `work/psd/seethrough/` に保存する。
2. `C:\00_PG\30_live` でSee-through出力をひより互換の詳細パーツ名へ変換する。
3. Photoshop 2026で顔下地、髪の塗り足し、左右の目、閉じ目、口中を目視修正する。
4. Cubism 5.3.03を左サブモニターで開き、公式ひよりテンプレートを使う。
5. 体格や顔型が異なるため、顔輪郭・胴体へテンプレートを一括適用しない。目・口・髪など対応が確認できたArtMeshだけを対象にする。
6. まばたき、口パク、髪揺れを端値で確認してからSDK 5 / SDK 4互換を書き出す。
7. `C:\00_PG\20_PicoAgent\characters\<character>` へ組み込み、上半身表示、透過、起動モニター、コンソール非表示を確認する。

## キャラクターごとに必ず変える項目

- リポジトリ名と配置先
- 元画像ファイル名
- PicoAgentのキャラクターID、表示名、サムネイル、Live2Dパス
- モデルの表示倍率と上下位置
- 顔・髪・衣装に合わせたマスク範囲
- ライセンスと権利情報

## 完了条件

- 元絵を直接上書きしていない
- PSDのパーツ名とCubism対応表が記録されている
- まばたき、口パク、髪揺れの目視確認済み
- SDK 5 / SDK 4互換の両出力が参照切れなし
- PicoAgentで上半身表示される
- `WORKFLOW.md` と `STATUS.md` が実際の工程に一致する
- Gitに制作物と最新版手順が同じコミットで残っている
